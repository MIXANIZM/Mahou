name: Apply second GDI hardening pass

on:
  pull_request:
    branches:
      - master
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: apply-gdi-second-pass-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: false

jobs:
  apply:
    if: github.head_ref == 'mixanizm-modern-v2.9.0.1' || github.ref_name == 'mixanizm-modern-v2.9.0.1'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout modern branch
        uses: actions/checkout@v4
        with:
          ref: mixanizm-modern-v2.9.0.1
          fetch-depth: 0
          show-progress: false

      - name: Apply second GDI ownership pass
        shell: bash
        run: |
          set -o pipefail
          mkdir -p gdi-second-pass-diagnostics
          script=.github/scripts/apply-gdi-second-pass.py
          if [ ! -f "$script" ]; then
            echo 'Second GDI pass is already applied.' | tee gdi-second-pass-diagnostics/result.txt
            exit 0
          fi
          python3 "$script" 2>&1 | tee gdi-second-pass-diagnostics/result.txt
          python3 .github/scripts/security-regression.py 2>&1 | tee -a gdi-second-pass-diagnostics/result.txt
          git config core.whitespace cr-at-eol
          git diff --check 2>&1 | tee -a gdi-second-pass-diagnostics/result.txt
          rm -f .github/workflows/apply-gdi-second-pass.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'fix: release lock-state and file icon GDI handles'
          git push origin HEAD:mixanizm-modern-v2.9.0.1

      - name: Upload second GDI diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: gdi-second-pass-diagnostics
          path: gdi-second-pass-diagnostics
          if-no-files-found: warn
          retention-days: 3
