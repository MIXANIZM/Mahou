name: Fix translation scope

on:
  pull_request:
    branches:
      - master
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: fix-translation-scope-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: false

jobs:
  apply:
    if: github.head_ref == 'mixanizm-modern-v2.9.0.1' || github.ref_name == 'mixanizm-modern-v2.9.0.1'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout modern branch
        uses: actions/checkout@v4
        with:
          ref: mixanizm-modern-v2.9.0.1
          fetch-depth: 0
          show-progress: false

      - name: Correct translator scopes
        shell: bash
        run: |
          set -o pipefail
          mkdir -p translation-scope-diagnostics
          script=.github/scripts/fix-translation-scope.py
          if [ ! -f "$script" ]; then
            echo 'Translator scope correction is already applied.' | tee translation-scope-diagnostics/result.txt
            exit 0
          fi
          python3 "$script" 2>&1 | tee translation-scope-diagnostics/result.txt
          python3 .github/scripts/security-regression.py 2>&1 | tee -a translation-scope-diagnostics/result.txt
          git config core.whitespace cr-at-eol
          git diff --check 2>&1 | tee -a translation-scope-diagnostics/result.txt
          rm -f .github/workflows/fix-translation-scope.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'fix: preserve translator scopes and bound speech download'
          git push origin HEAD:mixanizm-modern-v2.9.0.1

      - name: Upload translator scope diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: translation-scope-diagnostics
          path: translation-scope-diagnostics
          if-no-files-found: warn
          retention-days: 3
