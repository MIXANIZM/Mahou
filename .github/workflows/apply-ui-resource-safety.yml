name: Apply UI resource safety

on:
  pull_request:
    branches:
      - master
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: apply-ui-resource-safety-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: false

jobs:
  apply:
    if: github.head_ref == 'mixanizm-modern-v2.9.0.1' || github.ref_name == 'mixanizm-modern-v2.9.0.1'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout modern branch
        uses: actions/checkout@v4
        with:
          ref: mixanizm-modern-v2.9.0.1
          fetch-depth: 0
          show-progress: false

      - name: Apply UI resource lifetime hardening
        shell: bash
        run: |
          set -o pipefail
          mkdir -p ui-resource-diagnostics
          script=.github/scripts/apply-ui-resource-safety.py
          if [ ! -f "$script" ]; then
            echo 'UI resource hardening is already applied.' | tee ui-resource-diagnostics/result.txt
            exit 0
          fi
          python3 "$script" 2>&1 | tee ui-resource-diagnostics/result.txt
          python3 .github/scripts/security-regression.py 2>&1 | tee -a ui-resource-diagnostics/result.txt
          git config core.whitespace cr-at-eol
          git diff --check 2>&1 | tee -a ui-resource-diagnostics/result.txt
          rm -f .github/workflows/apply-ui-resource-safety.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'fix: harden UI graphics and temporary form lifetimes'
          git push origin HEAD:mixanizm-modern-v2.9.0.1

      - name: Upload UI resource diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ui-resource-diagnostics
          path: ui-resource-diagnostics
          if-no-files-found: warn
          retention-days: 3
