# Privacy defaults

The following features require explicit opt-in on a clean MIXANIZM Mahou profile:

- input history;
- online translation;
- AutoSwitch;
- detailed logging.

Existing explicit user choices are preserved. The proxy password is protected for the current Windows user with DPAPI and masked in the settings interface. Full OLE clipboard preservation is mandatory; the former text-only backup mode is disabled.
